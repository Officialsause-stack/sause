// Simple Add-to-Cart functionality
let cart = [];

function addToCart() {
    const product = {
        name: "1 Sause Official Cosmetic",
        price: 20, // Placeholder price in USD
        quantity: 1
    };
    cart.push(product);
    renderCart();
}

function renderCart() {
    const cartItems = document.getElementById('cart-items');
    cartItems.innerHTML = '';
    let total = 0;
    cart.forEach((item, index) => {
        const itemEl = document.createElement('div');
        itemEl.innerHTML = `${item.name} - $${item.price} <button onclick="removeFromCart(${index})">Remove</button>`;
        cartItems.appendChild(itemEl);
        total += item.price;
    });
    document.getElementById('cart-total').innerText = `Total: $${total}`;
}

function removeFromCart(index) {
    cart.splice(index, 1);
    renderCart();
}

// Checkout function placeholder
function checkout() {
    if(cart.length === 0) {
        alert("Your cart is empty!");
        return;
    }
    alert("Stripe & PayPal integration placeholder. Replace with real API keys to enable payment.");
}
